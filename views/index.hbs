<!doctype html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/style.css">
  </head>
  <body>
    <h1>Setup</h1>
    <p>Pick the conjugations you'd like to be tested on:
    <form action="/test" method="get">
      {{#each conjugations}}
      <div>
        <input type="checkbox" name="{{this}}" value="true">
        <label>{{this}}</label>
      </div>
      {{/each}}
      <input name="settings" value="true" type="hidden">
      <button type="submit">begin</button>
    </form>
  </body>
</html>
